name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    # steps:
    #   # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
    #   - uses: actions/checkout@v3

    #   - name: Run intall all dependencies
    #     run: cd app && npm install

    #   - name: Run a test
    #     run: cd app && npm run test

    #   - name: Build Docker image
    #     run: docker build -t ${{ secrets.DOCKERHUB_USER }}/assez-dechets-back .

    #   - name: DockerHub login
    #     run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u cottonmouthh --password-stdin

    #   - name: Push to DockerHub
    #     run: docker push ${{ secrets.DOCKERHUB_USER }}/assez-dechets-back
    # steps:
    #   - name: "gcr.io/cloud-builders/docker"
    #     args: ["build", "-t", "$_REGISTRY/$_K_SERVICE:$SHORT_SHA", "."]
    #   - name: "gcr.io/cloud-builders/docker"
    #     args: ["push", "$_REGISTRY/$_K_SERVICE:$SHORT_SHA"]
    #   - name: "gcr.io/cloud-builders/gcloud"
    #     args:
    #       [
    #         "run",
    #         "deploy",
    #         "--image",
    #         "$_REGISTRY/$_K_SERVICE:$SHORT_SHA",
    #         "--revision-suffix",
    #         "$SHORT_SHA",
    #         "--no-traffic",
    #         "--region",
    #         "$LOCATION",
    #         "$_K_SERVICE",
    #       ]
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: "test ci"
        run: npm run test
    # images: ["$_REGISTRY/$_K_SERVICE"]
